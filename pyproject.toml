[project]
name = "program-pm"
version = "0.1.0"
description = "Windows project manager GUI for ModuleFiles and GitHub workflows."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
program-pm = "program_pm.app:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
